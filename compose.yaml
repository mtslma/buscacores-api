services:
  postgres:
    image: 'postgres:latest'
    container_name: 'postgres-buscacores'
    environment:
      - 'POSTGRES_DB=buscacores_db'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=admin'
    ports:
      - '5432'
